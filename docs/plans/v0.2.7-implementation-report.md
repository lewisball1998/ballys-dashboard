# v0.2.7 — Icon Alias Expansion & Generic Icon Set — Implementation Report

**Phase:** v0.2.7 — Icon Alias Expansion & Generic Icon Set
**Branch:** `feature/v0.2.7-icon-alias-expansion` (off latest `main` `a88aef9`)
**Status:** Implemented, committed, and pushed. Not merged or tagged.

---

## Report

- **Current branch:** `feature/v0.2.7-icon-alias-expansion`
- **Latest commit hash:** `9a9f0de4132334ada78dc68dfc22afc2f5ea8a23`
  (`feat(icons): icon alias expansion & generic icon set (v0.2.7)`)
- **Working tree clean:** Yes — all changes committed; in sync with `origin`.
- **Branch pushed:** Yes — `origin/feature/v0.2.7-icon-alias-expansion` with upstream
  tracking. PR: https://github.com/lewisball1998/ballys-dashboard/pull/new/feature/v0.2.7-icon-alias-expansion

### Files changed
21 files (7 modified, 14 new) + the carried-over research plan doc.

**Modified**
- `src/lib/icons/registry.ts` — 16 → 30 keys, ~550 aliases (each unique to one key)
- `src/lib/icons/suggest.ts` — qualifier-strip pre-pass + short-name bleed guard
- `tests/unit/icon-suggest.test.ts` — full approved-mapping table + strip/non-bleed cases
- `src/lib/constants.ts` — `APP_VERSION` → `0.2.7`
- `package.json` — version → `0.2.7`
- `CHANGELOG.md` — 0.2.7 entry
- `docs/adr/README.md` — index row for ADR 0014

**New**
- 14 first-party monochrome SVGs in `public/icons/builtin/`: `ai, proxy, identity,
  automation, camera, backup, notes, finance, rss, server, indexer, calendar,
  search, requests`
- `docs/adr/0014-icon-policy-and-pack-import.md`
- `docs/plans/v0.2.7-icon-pack-research-alias-expansion-plan.md` (research basis)

**Untouched (scope guards verified — not in diff)**
- `src/db/schema/apps.ts`, `Dockerfile`, `docker-compose.yml`, auth files,
  `src/lib/icons/resolve.ts`, `src/components/launcher/app-icon.tsx`,
  `src/components/launcher/icon-picker.tsx`

### Checks run
- `pnpm typecheck` → ✅ 0
- `pnpm lint` → ✅ "No ESLint warnings or errors"
- `pnpm test` → ✅ **321 passed** (35 files; +56 new icon-suggest cases)
- `pnpm build` → ✅ 0
- `pnpm prettier --check` (changed files) → ✅ clean

### DB / migration result
`pnpm db:generate` → **"No schema changes, nothing to migrate"**; migrations dir
unchanged (`0000`–`0003`). No new table, no migration.

### Docker result
No Docker/deployment changes (none in diff). Not run this phase (pure code + assets;
nothing Docker-affecting changed).

### Health checks
Not re-run — no server/runtime/health code touched. Build serves all 30 builtin glyphs.

### Warnings / errors
Non-blocking only — `next lint` deprecation notice; repo-wide `format:check` debt
pre-exists (changed files are prettier-clean). Two issues were caught and fixed during
implementation before commit:
1. Registry would have left `Uptime Kuma` on `chart` → re-homed `uptime`/`uptimekuma`/
   `netdata`/`monitoring` to `monitor`.
2. Short-name bleed (`"HD"` → `database` via `couchdb` containing "hd") → fixed with a
   ≥ 3-char residual guard on the weakest match signal.

### QA verdict
Implemented exactly to the approved scope. All approved mappings asserted and passing,
including:
`Sonarr 4K`/`Radarr-Anime` → `media`; `Ollama`/`Open WebUI`/`AnythingLLM`/`ComfyUI` →
`ai`; `Authentik`/`Vaultwarden`/`Keycloak`/`Authelia` → `identity`; `Traefik`/`Caddy`/
`Nginx Proxy Manager` → `proxy`; `Frigate`/`Scrypted` → `camera`; `Proxmox` → `server`;
`TrueNAS` → `storage`; `Prowlarr`/`Jackett` → `indexer`; `Firefly III`/`Actual Budget`
→ `finance`; `FreshRSS` → `rss`; `SearXNG` → `search`; `Joplin` → `notes`; `Duplicati`
→ `backup`; `Node-RED`/`ESPHome` → `automation`; `Home Assistant` → `home`.
No duplicate aliases (549 unique across 30 keys). `suggestIconKey` stays pure (returns a
base key only; no `?v=4k`). Auto-suggest "never overwrite explicit choice" remains
enforced by the picker (gates on empty value — unchanged from v0.2.6).

### Recommendation
Ready for QA/merge. Suggested manual pass before tagging: open the icon picker in **dark
+ light mode** and eyeball the 14 new glyphs at small sizes (hand-authored — `camera`,
`requests`, and `backup` are the most intricate; flag any that read poorly for SVG
refinement). Then the usual gate (typecheck/lint/test/build/db:generate-no-changes) and
merge/tag `v0.2.7`. Icon-pack import (`pack:<id>/<key>`) remains designed-only in ADR
0014 for a future phase — not started, per scope.
